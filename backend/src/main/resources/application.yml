server:
  port: 8080

spring:
  jackson:
    serialization:
      indent_output: true
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

builder:
  metro:
    enabled: true
    datasource:
      url: jdbc:mysql://test-metro-db.cyi4arp1bouk.eu-west-1.rds.amazonaws.com:3306/metro?useSSL=true&requireSSL=true
      username: mastermetro
      password: DOQmUyfeuOQp3TIjr4NJ
      driver-class-name: com.mysql.cj.jdbc.Driver
  google:
    apiKey: ${GOOGLE_API_KEY:}
  auth:
    enabled: ${BUILDER_AUTH_ENABLED:false}
    username: ${BUILDER_AUTH_USER:tester}
    password: ${BUILDER_AUTH_PASSWORD:}
    tokenSecret: ${BUILDER_AUTH_TOKEN_SECRET:changeme-in-production}
